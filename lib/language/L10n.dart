import 'dart:ui';

import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class L10n {
  static final all = [
    const Locale("en"),
    const Locale("it"),
    const Locale("es"),
    const Locale("fr"),
    const Locale("de"),
  ];
}
